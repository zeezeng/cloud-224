-- ============================================================
-- Flyway 初始建表脚本（V1）
-- ------------------------------------------------------------
-- 包含项目所有表的 CREATE TABLE 语句
-- 全新数据库启动时 Flyway 从 V1 开始自动执行
--
-- 历史说明：
--   早期版本中本脚本仅为占位基线（不执行建表），
--   依赖手动导入 schema。现已整合全部建表语句，
--   实现「一份脚本，全新部署」。
--
-- 后续数据库结构变更规范：
--   新增 V2__xxx.sql、V3__xxx.sql ... 放入本目录，
--   后端启动时 Flyway 会按版本号顺序自动执行未应用的脚本。
--   脚本命名：V<版本号>__<简短描述>.sql（双下划线分隔）
-- ============================================================

