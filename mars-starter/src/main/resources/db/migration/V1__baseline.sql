-- ============================================================
-- Flyway 基线脚本（V1）
-- ------------------------------------------------------------
-- baseline-version=1：本脚本不会被 Flyway 执行
--   首次启动时 baseline-on-migrate 会把现有数据库直接标记为 V1 已应用，
--   不会触碰任何已有表结构。
--
-- 当前基线已包含的手动变更（集成 Flyway 之前已执行）：
--   * app_notice 表新增 notice_type / valid_from / valid_to 字段
--   * app_notice.title 改为允许 NULL
--
-- 后续数据库结构变更规范：
--   新增 V2__xxx.sql、V3__xxx.sql ... 放入本目录，
--   后端启动时 Flyway 会按版本号顺序自动执行未应用的脚本。
--   脚本命名：V<版本号>__<简短描述>.sql（双下划线分隔）
-- ============================================================
