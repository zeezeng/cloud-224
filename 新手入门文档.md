# Mars Admin 项目新手入门文档

> 写给零基础小白：用最通俗的语言，带你搞懂这个项目是什么、怎么运作、每个目录干什么、代码怎么改。

---

## 一、先搞懂几个基础概念

在开始之前，先解释几个会反复出现的词。看不懂没关系，有个印象就行，后面结合项目再看就明白了。

### 1. 什么是 Maven？

**Maven** 是 Java 项目的"管家"，类似前端的 npm。

- 你写 Java 代码时，会用到别人写好的工具库（比如操作数据库的工具、加密工具）。如果手动下载 jar 包放到项目里，非常麻烦。
- Maven 帮你做两件事：
  1. **管依赖**：你在 `pom.xml` 文件里写一句"我需要某个库"，Maven 自动去网上下载。
  2. **管构建**：一条命令 `mvn clean install` 就能编译、打包所有代码。

本项目是一个 **Maven 多模块项目**，也就是一个大项目拆成多个小模块（子项目），每个模块有自己的 `pom.xml`，最终组装成一个完整的系统。

### 2. 什么是 Spring / Spring Boot？

- **Spring** 是 Java 世界最流行的框架。它的核心思想是：你不用自己 `new` 对象，让 Spring 容器帮你创建和管理对象（这叫"依赖注入"）。
- **Spring Boot** 是 Spring 的"快速启动版"。以前用 Spring 要写一大堆配置文件，Spring Boot 约定大于配置，开箱即用，一个 `main` 方法就能启动整个 Web 项目。

本项目用 **Spring Boot 3.2.2**，启动入口在 [MarsAdminApplication.java](file:///f:/MyCodes/cloud-224/mars-starter/src/main/java/com/mars/MarsAdminApplication.java)。

### 3. 什么是前后端分离？

传统方式：后端渲染整个页面返回 HTML。
前后端分离：后端只提供数据接口（API），前端自己渲染页面。

- **后端**（Java）：负责存数据、处理业务逻辑、返回 JSON 数据。
- **前端**（Vue）：负责展示页面、用户交互、调用后端接口拿数据。

本项目就是前后端分离：
- 后端接口地址带 `/api` 前缀，返回 JSON。
- 前端用 Vue 写页面，通过 axios 调用后端接口。

### 4. 什么是 Vue / Vite / Pinia？

- **Vue**：前端框架，用组件化方式写页面。一个 `.vue` 文件就是一个组件。
- **Vite**：前端构建工具，开发时启动飞快，负责把 Vue 代码打包成浏览器能识别的 HTML/JS/CSS。
- **Pinia**：Vue 的状态管理库，类似全局变量仓库，多个页面共享数据时用。
- **Naive UI**：现成的 UI 组件库（按钮、表格、弹窗等），不用自己从零写样式。

### 5. 什么是 MyBatis-Plus？

Java 操作数据库的工具。你写一个 Java 类（实体类）对应一张数据库表，写一个 Mapper 接口就能增删改查，不用手写大量 SQL。MyBatis-Plus 是 MyBatis 的增强版，自带分页、逻辑删除等功能。

### 6. 什么是 Sa-Token？

权限认证框架。管三件事：
- 你登录了吗（认证）
- 你能访问哪些菜单/接口（权限）
- 你的登录状态怎么保存（用 Redis 存 token）

---

## 二、这个项目到底是干什么的？

**Mars Admin** 是一个**后台管理系统**的基础框架，开箱即用。

简单说，就是那种"管理员登录后能管理用户、角色、菜单、看日志、发公告"的系统。你可以把它当模板，往里面加自己的业务功能。

本项目在此基础上，还做了一个 **"云224主播管理"** 业务（在 `yun` 目录下），用来管理直播主播数据、同步礼物统计等。

### 整体技术栈一览

| 层 | 技术 | 作用 |
|---|---|---|
| 后端框架 | Spring Boot 3.2 | Web 服务、依赖注入 |
| 数据库 | MySQL 8 | 存数据 |
| 缓存 | Redis 7 | 存登录 token、热点数据 |
| ORM | MyBatis-Plus | 操作数据库 |
| 权限 | Sa-Token | 登录认证、权限控制 |
| 定时任务 | Quartz | 定时执行任务 |
| 数据库迁移 | Flyway | 自动执行 SQL 脚本升级数据库 |
| 前端框架 | Vue 3 + TypeScript | 写页面 |
| 前端构建 | Vite 5 | 打包 |
| UI 库 | Naive UI | 现成组件 |
| 状态管理 | Pinia | 全局数据 |
| 移动端 | UniApp | 小程序（多端） |

---

## 三、项目整体结构（鸟瞰图）

项目根目录 `f:\MyCodes\cloud-224` 下有几个大目录：

```
cloud-224/
├── mars-common/      ← 公共基础模块（工具类、统一返回结果）
├── mars-infra/       ← 基础设施层（数据库、Redis、文件存储、短信、支付等）
├── mars-core/        ← 业务核心层（系统管理、认证、消息、文件、代码生成、biz业务）
├── mars-api/         ← 接口层（Controller，对外提供 API）
├── mars-job/         ← 定时任务模块
├── mars-starter/     ← 启动入口（main 方法、配置文件、数据库迁移脚本）
├── mars-ui/          ← PC 管理后台前端（Vue3）
├── mars-uniapp/      ← 移动端小程序（UniApp）
├── doc/              ← 文档和截图
├── pom.xml           ← Maven 根配置（管理所有子模块）
└── README.md         ← 项目说明
```

### 一个请求是怎么走完整个项目的？（重要！）

记住这张图，后面所有代码都是为了实现这个流程：

```
用户在浏览器点按钮
   ↓
Vue 页面（mars-ui/src/views/）调用 API
   ↓
前端 API 封装（mars-ui/src/api/xxx.ts）→ axios 发起 HTTP 请求
   ↓
（经过 Vite 代理 /api → 后端 8080 端口）
   ↓
后端 Controller（mars-api/.../controller/）接收请求
   ↓
Controller 调用 Service（mars-core/.../service/）处理业务
   ↓
Service 调用 Mapper（mars-core/.../mapper/）操作数据库
   ↓
MySQL 返回数据 → Service 处理 → Controller 包装成 Result → 返回 JSON
   ↓
前端接收 JSON → 渲染到页面
```

---

## 四、后端各模块详解

### 1. `mars-starter` —— 启动入口 ⭐

整个程序的"大门"。运行这里的 main 方法，整个系统就启动了。

关键文件：
- [MarsAdminApplication.java](file:///f:/MyCodes/cloud-224/mars-starter/src/main/java/com/mars/MarsAdminApplication.java) —— 启动类，包含 `main` 方法
- [application.yml](file:///f:/MyCodes/cloud-224/mars-starter/src/main/resources/application.yml) —— 主配置，只指定了用 `dev` 环境
- [application-dev.yml](file:///f:/MyCodes/cloud-224/mars-starter/src/main/resources/application-dev.yml) —— 开发环境配置（数据库地址、Redis 地址、端口等）
- `application-prod.yml` —— 生产环境配置（上线时用）
- `db/migration/` —— **Flyway 数据库迁移脚本**，启动时自动执行

> ⚠️ **重要约定**：本项目用 Flyway 管理数据库结构。要改表结构，**不要**手动去数据库改，而是在 `db/migration/` 下新建脚本，命名规则 `V版本号__描述.sql`（如 `V4__add_xxx.sql`）。下次启动项目，Flyway 会自动执行。

### 2. `mars-common` —— 公共基础

所有模块都会用到的基础代码，不依赖任何业务。

| 目录/文件 | 作用 |
|---|---|
| [Result.java](file:///f:/MyCodes/cloud-224/mars-common/src/main/java/com/mars/common/result/Result.java) | 统一返回结果，所有接口都返回 `{code, message, data}` 格式 |
| `PageResult.java` | 分页返回结果 |
| `BaseEntity.java` | 实体类基类（包含 id、创建时间等公共字段） |
| `BusinessException.java` | 业务异常 |
| `GlobalExceptionHandler.java` | 全局异常处理（出错了统一返回友好提示） |
| `RsaUtils.java` | RSA 加密工具 |

**举例**：后端接口返回数据都用这个格式（[Result.java](file:///f:/MyCodes/cloud-224/mars-common/src/main/java/com/mars/common/result/Result.java)）：

```java
return Result.ok(data);      // 成功，返回数据
return Result.fail("失败原因"); // 失败
```

前端收到的 JSON 就是：
```json
{ "code": 200, "message": "操作成功", "data": {...} }
```

### 3. `mars-infra` —— 基础设施层

提供各种"能力"的模块，跟具体业务无关，可以理解为工具箱：

| 子模块 | 作用 |
|---|---|
| `mars-db` | 数据库配置（MyBatis-Plus 配置、数据权限拦截器） |
| `mars-redis` | Redis 配置 |
| `mars-oss` | 文件存储（本地/MinIO/阿里云OSS/腾讯云COS，策略工厂模式可切换） |
| `mars-sms` | 短信服务（阿里云/腾讯云/控制台） |
| `mars-pay` | 支付（微信/支付宝） |
| `mars-push` | 推送（钉钉/飞书/企业微信/Webhook） |
| `mars-mail` | 邮件 |
| `mars-social` | 社交登录（微信/支付宝/苹果） |
| `mars-wechat` | 微信公众号/小程序 |
| `mars-websocket` | WebSocket 支持（实时通讯） |
| `mars-crypto` | 接口加密解密（RSA + AES） |

**策略工厂模式**：比如文件存储，你想存本地就存本地，想存 OSS 就存 OSS。代码里定义一个 `FileStorage` 接口，不同实现类对应不同存储方式。配置里切换一下就行，不用改代码。

### 4. `mars-core` —— 业务核心层

这里放具体的业务逻辑，分几个子模块：

#### `mars-system` —— 系统管理（核心中的核心）

包含 RBAC 权限体系：用户、角色、菜单、部门、岗位、字典、配置、日志等。

- `entity/` —— 实体类（对应数据库表），如 `SysUser`（用户）、`SysRole`（角色）、`SysMenu`（菜单）
- `mapper/` —— MyBatis 接口，操作数据库
- `service/` —— 业务逻辑
- `annotation/` —— 自定义注解，如 `@Log`（记录操作日志）、`@RepeatSubmit`（防重复提交）
- `aspect/` —— AOP 切面，自动执行日志记录等
- `config/` —— Sa-Token 配置、权限配置

#### `mars-auth` —— 认证授权

各种登录方式的实现（策略模式）：

- `strategy/PasswordLoginStrategy.java` —— 账号密码登录
- `strategy/SmsCodeLoginStrategy.java` —— 短信验证码登录
- `strategy/MiniProgramLoginStrategy.java` —— 小程序登录
- `strategy/SocialLoginStrategy.java` —— 社交登录

要加新的登录方式？新建一个策略类实现 `LoginStrategy` 接口即可。

#### `mars-biz` —— 云224业务（主播管理）

这是本项目特有的业务模块，管理直播主播：

- `entity/YunAnchor.java` —— 主播实体（[看这里](file:///f:/MyCodes/cloud-224/mars-core/mars-biz/src/main/java/com/mars/biz/entity/YunAnchor.java)）
- `service/YunAnchorService.java` —— 主播业务逻辑
- `service/DoseeingClient.java` —— 调用"在看"平台接口同步主播数据
- `entity/YunAnchorGiftStat.java` —— 礼物统计
- `entity/YunSyncLog.java` —— 同步日志

#### 其他子模块

- `mars-file` —— 文件管理
- `mars-gen` —— 代码生成器（根据数据库表自动生成增删改查代码）
- `mars-message` —— 消息中心（公告、聊天、群聊）

### 5. `mars-api` —— 接口层（Controller）

这里是真正暴露给前端调用的接口。按客户端类型分三个子模块：

| 子模块 | 给谁用 | 路径 |
|---|---|---|
| `mars-admin-api` | PC 后台管理页面 | `/api/...` |
| `mars-app-api` | APP/小程序 | `/api/app/...` |
| `mars-web-api` | 网页端 | `/api/web/...` |

#### `mars-admin-api` 目录结构

```
mars-admin-api/src/main/java/com/mars/admin/
├── config/           ← 配置（API 前缀、演示模式拦截器、Web 配置）
├── controller/       ← 接口控制器（重点！）
│   ├── auth/         ← 登录认证接口
│   ├── system/       ← 系统管理接口（用户、角色、菜单...）
│   ├── monitor/      ← 监控接口（在线用户、定时任务、服务器...）
│   ├── message/      ← 消息接口
│   ├── file/         ← 文件接口
│   ├── gen/          ← 代码生成接口
│   └── yun/          ← 云224主播管理接口
├── runner/           ← 启动后执行的任务
└── websocket/        ← WebSocket 配置（聊天、SSH 终端）
```

**重点**：[ApiPrefixConfig.java](file:///f:/MyCodes/cloud-224/mars-api/mars-admin-api/src/main/java/com/mars/admin/config/ApiPrefixConfig.java) 给所有 Controller 自动加了 `/api` 前缀。所以你在 Controller 里写 `@RequestMapping("/yun/anchor")`，实际访问路径是 `/api/yun/anchor`。

### 6. `mars-job` —— 定时任务

基于 Quartz 的定时任务模块。比如每天凌晨自动同步主播数据。

---

## 五、前端（mars-ui）详解

### 目录结构

```
mars-ui/
├── src/
│   ├── api/          ← 后端接口封装（每个文件对应一类业务）
│   ├── components/   ← 公共组件
│   ├── layout/       ← 整体布局（侧边栏 + 顶栏 + 内容区）
│   ├── router/       ← 路由配置（URL 对应哪个页面）
│   ├── stores/       ← Pinia 状态管理
│   ├── styles/       ← 全局样式
│   ├── utils/        ← 工具函数（请求、加密、WebSocket...）
│   ├── views/        ← 页面组件（重点！）
│   ├── App.vue       ← 根组件
│   └── main.ts       ← 入口文件
├── vite.config.ts    ← Vite 配置（含代理配置）
├── package.json      ← 依赖和脚本
└── tsconfig.json     ← TypeScript 配置
```

### 关键文件解读

#### 1. [main.ts](file:///f:/MyCodes/cloud-224/mars-ui/src/main.ts) —— 入口

启动 Vue 应用，加载 Pinia、路由、Naive UI，预加载加密配置和站点配置。

#### 2. [utils/request.ts](file:///f:/MyCodes/cloud-224/mars-ui/src/utils/request.ts) —— HTTP 请求封装 ⭐

**这个文件非常重要，所有前端 API 调用都经过它。** 它做了几件事：

- 创建 axios 实例，`baseURL` 设为 `/api`
- **请求拦截器**：自动在请求头加上 `Authorization`（登录 token）
- **响应拦截器**：
  - 统一处理后端返回的 `{code, message, data}` 格式
  - `code !== 200` 时弹错误提示
  - `code === 401` 时自动退出登录跳转登录页
  - 自动解密 AES 加密的响应数据

#### 3. [api/anchor.ts](file:///f:/MyCodes/cloud-224/mars-ui/src/api/anchor.ts) —— 主播接口封装

示例：调用后端接口就这么简单
```typescript
import { request } from '@/utils/request'

export const anchorApi = {
  page(params) {
    return request({ url: '/yun/anchor/page', method: 'get', params })
  },
  create(data) {
    return request({ url: '/yun/anchor', method: 'post', data })
  }
}
```

#### 4. [router/index.ts](file:///f:/MyCodes/cloud-224/mars-ui/src/router/index.ts) —— 路由 ⭐

定义"网址 → 页面"的对应关系。比如 `/dashboard` 对应首页，`/system/user` 对应用户管理。

特别之处：**动态路由**。登录后根据用户的菜单权限，自动添加路由（`addDynamicRoutes` 函数）。所以菜单权限是在数据库里配置的，不是写死在前端代码里。

#### 5. `views/` —— 页面组件

按业务分目录：

| 目录 | 内容 |
|---|---|
| `dashboard/` | 首页控制台 |
| `login/` | 登录页 |
| `system/` | 系统管理（用户、角色、菜单、字典、配置、文件...） |
| `monitor/` | 系统监控（在线用户、定时任务、缓存、服务器...） |
| `log/` | 日志（操作日志、登录日志） |
| `message/` | 消息中心（公告、聊天） |
| `org/` | 组织管理（部门、岗位） |
| `tool/` | 工具（代码生成） |
| `yun/` | 云224业务（主播、轮播图、公告） |

### Vite 配置里的代理 ⚠️

[vite.config.ts](file:///f:/MyCodes/cloud-224/mars-ui/vite.config.ts) 配置了开发时代理：

```typescript
server: {
  port: 3000,                              // 前端跑在 3000 端口
  proxy: {
    '/api': 'http://localhost:8080',       // API 请求转发到后端 8080
    '/druid': 'http://localhost:8080',     // 数据库监控
    '/ws': 'ws://localhost:8080'           // WebSocket
  }
}
```

这就是为什么前端代码里写 `/api/xxx`，实际会请求到后端 `8080` 端口，**避开了浏览器的跨域问题**。

---

## 六、配置文件详解

### 后端配置 [application-dev.yml](file:///f:/MyCodes/cloud-224/mars-starter/src/main/resources/application-dev.yml)

```yaml
server:
  port: 8080                    # 后端端口

spring:
  datasource:                   # 数据库配置
    url: jdbc:mysql://localhost:3306/mars-system  # 数据库地址
    username: root
    password: root              # ← 改成你自己的数据库密码
  data:
    redis:                      # Redis 配置
      host: localhost
      port: 6379
      database: 6               # 用第 6 号数据库
  flyway:                       # 数据库迁移
    enabled: true
    locations: classpath:db/migration

mybatis-plus:                   # ORM 配置
  mapper-locations: classpath*:mapper/**/*.xml  # SQL 映射文件位置
  global-config:
    db-config:
      logic-delete-field: deleted        # 逻辑删除字段（deleted=1 表示已删除）
      logic-delete-value: 1
      logic-not-delete-value: 0

mars:
  demo-mode: false              # 演示模式（true 时禁止增删改）
```

**新手常见问题：项目跑不起来？**
- 90% 是数据库连不上。检查 MySQL 是否启动、密码是否正确、数据库 `mars-system` 是否创建。
- Redis 是否启动。

### 前端配置 `mars-ui/`

- `package.json` —— 依赖和启动命令
- `vite.config.ts` —— Vite 配置（端口、代理、打包输出目录）
- `tsconfig.json` —— TypeScript 配置

---

## 七、代码怎么读？一个完整例子

以"主播管理"为例，从头到尾走一遍：

### 第 1 步：数据库表

`yun_anchor` 表（[实体类 YunAnchor.java](file:///f:/MyCodes/cloud-224/mars-core/mars-biz/src/main/java/com/mars/biz/entity/YunAnchor.java)）

```java
@TableName("yun_anchor")        // 对应数据库表名
public class YunAnchor extends BaseEntity {
    private String anchorId;     // 字段对应表列
    private String anchorName;
    // ...其他字段
}
```

### 第 2 步：Mapper 接口（操作数据库）

`YunAnchorMapper.java` 继承 MyBatis-Plus 的 `BaseMapper`，自带增删改查方法，不用写 SQL。

### 第 3 步：Service（业务逻辑）

[YunAnchorService.java](file:///f:/MyCodes/cloud-224/mars-core/mars-biz/src/main/java/com/mars/biz/service/YunAnchorService.java) 定义业务方法，实现类里写具体逻辑。

### 第 4 步：Controller（对外接口）

[YunAnchorController.java](file:///f:/MyCodes/cloud-224/mars-api/mars-admin-api/src/main/java/com/mars/admin/controller/yun/YunAnchorController.java)：

```java
@RestController                      // 标记为 REST 接口
@RequestMapping("/yun/anchor")       // 路径（实际是 /api/yun/anchor）
@RequiredArgsConstructor
public class YunAnchorController {

    private final YunAnchorService yunAnchorService;  // 注入 Service

    @GetMapping("/page")             // GET 请求
    @SaCheckPermission("yun:anchor:list")  // 权限校验：需要 yun:anchor:list 权限
    public Result<PageResult<YunAnchorPageRow>> page(...) {
        var result = yunAnchorService.page(...);  // 调 Service
        return Result.ok(PageResult.of(result));  // 包装成统一格式返回
    }

    @PostMapping                     // POST 请求（新增）
    @SaCheckPermission("yun:anchor:add")
    @Log(title = "云224主播", businessType = BusinessType.INSERT)  // 自动记录操作日志
    public Result<Void> add(@RequestBody YunAnchor anchor) {
        yunAnchorService.create(anchor);
        return Result.ok();
    }
}
```

**注解含义**：
- `@RestController` = 告诉 Spring 这个类是接口控制器，返回值自动转 JSON
- `@RequestMapping` = 这个接口的 URL 路径
- `@GetMapping` / `@PostMapping` = 处理 GET/POST 请求
- `@SaCheckPermission` = 校验权限，没权限的人调用会报错
- `@Log` = 自动记录操作日志（谁在什么时间做了什么）
- `@RequestBody` = 把请求体的 JSON 自动转成 Java 对象
- `@PathVariable` = 从 URL 路径取参数（如 `/yun/anchor/123` 里的 123）
- `@RequestParam` = 从 URL 查询参数取（如 `?page=1`）

### 第 5 步：前端调用

[api/anchor.ts](file:///f:/MyCodes/cloud-224/mars-ui/src/api/anchor.ts)：

```typescript
export const anchorApi = {
  page(params) {
    return request({ url: '/yun/anchor/page', method: 'get', params })
  }
}
```

### 第 6 步：前端页面

`views/yun/anchor/index.vue` 调用 `anchorApi.page()`，拿到数据后用 Naive UI 的表格展示。

---

## 八、新手怎么动手改？

### 场景 1：改个文案

比如想把"用户管理"改成"成员管理"：
- **菜单名**：去数据库 `sys_menu` 表改 `name` 字段，或在后台"系统管理 → 菜单管理"页面改。
- **页面标题**：去对应的 `views/system/user/index.vue` 找标题文字改。

### 场景 2：改后端接口逻辑

1. 找到对应的 Controller（在 `mars-api/mars-admin-api/.../controller/`）
2. 看它调用哪个 Service 方法
3. 进 Service 实现类改逻辑
4. 重启后端（dev 模式下 spring-boot-devtools 会自动重启）

### 场景 3：改前端页面

1. 找到对应的 `.vue` 文件（在 `mars-ui/src/views/`）
2. 改完保存，Vite 会自动热更新（浏览器不用手动刷新）

### 场景 4：加一个新接口

后端：
1. 在 `mars-core/.../entity/` 加实体类
2. 在 `mars-core/.../mapper/` 加 Mapper 接口
3. 在 `mars-core/.../service/` 加 Service 接口和实现
4. 在 `mars-api/mars-admin-api/.../controller/` 加 Controller

前端：
1. 在 `mars-ui/src/api/` 加接口封装
2. 在 `mars-ui/src/views/` 加页面
3. 在 `mars-ui/src/router/index.ts` 加路由（或通过菜单管理动态加）

### 场景 5：改数据库表结构

**不要直接改数据库！** 在 `mars-starter/src/main/resources/db/migration/` 新建 SQL 脚本：

```
V4__add_xxx_column.sql    ← 命名规则：V版本号__描述.sql
```

内容示例：
```sql
ALTER TABLE yun_anchor ADD COLUMN new_field VARCHAR(100) COMMENT '新字段';
```

重启项目，Flyway 会自动执行这个脚本。

---

## 九、启动项目（开发环境）

### 准备工作

1. 安装 **JDK 17+**
2. 安装 **Maven 3.8+**
3. 安装 **MySQL 8.0+**，创建数据库 `mars-system`
4. 安装 **Redis 7.0+**
5. 安装 **Node.js 18+**

### 启动后端

1. 改 [application-dev.yml](file:///f:/MyCodes/cloud-224/mars-starter/src/main/resources/application-dev.yml) 里的数据库密码、Redis 密码（如果有）
2. 在项目根目录执行：
   ```bash
   mvn clean install         # 编译所有模块
   cd mars-starter
   mvn spring-boot:run       # 启动后端
   ```
3. 后端跑在 `http://localhost:8080`
4. 首次启动时，Flyway 会自动建表并导入初始数据

### 启动前端

```bash
cd mars-ui
npm install                # 或 pnpm install
npm run dev                # 启动开发服务器
```

前端跑在 `http://localhost:3000`，打开浏览器访问即可。

### 默认账号

| 账号 | 密码 | 说明 |
|---|---|---|
| admin | admin123 | 超级管理员 |

---

## 十、几个关键约定（项目记忆）

这些是本项目特有的约定，改代码时要注意：

1. **数据库变更必须用 Flyway 脚本**，放在 `mars-starter/src/main/resources/db/migration/`，命名 `V版本号__描述.sql`。
2. **主播数据同步只用"在看"（DOSEEING）平台**，"播酱"相关代码已移除。
3. **后端接口加密白名单**必须包含 `/app/notice/`，否则公告接口返回加密数据客户端解不开。
4. **AppNotice 公告管理**中 `title` 字段非必填，客户端优先用 `content` 展示。
5. **主播同步平台选择**是工具栏全局选择，不绑定单个主播。
6. **演示模式**（`mars.demo-mode: true`）下禁止增删改操作，上线演示时可以开启。

---

## 十一、遇到问题怎么办？

| 问题 | 排查方向 |
|---|---|
| 后端启动报错 | 看 console 报错信息。常见：数据库连不上、Redis 没启动、端口被占用 |
| 前端接口调不通 | 检查后端是否启动、Vite 代理是否配置正确、接口路径是否对 |
| 接口返回 401 | 登录过期了，重新登录。检查 token 是否正确传递 |
| 接口返回 403 | 没权限。去"角色管理"给当前角色分配对应菜单权限 |
| 页面空白 | 按 F12 看 console 报错，多半是路由或组件路径不对 |
| 改了代码不生效 | 后端检查是否重启（devtools 是否生效）；前端检查是否热更新 |

---

## 十二、学习建议

1. **先把项目跑起来**：照着第九节操作，能跑起来就有成就感。
2. **从页面反推代码**：打开"用户管理"页面 → F12 看网络请求 → 找到接口路径 → 在后端搜对应 Controller → 顺着看 Service → 看 Mapper。
3. **小改练手**：试着改个按钮文字、改个字段名，跑通整个流程。
4. **看懂一个再看下一个**：别贪多，先把"用户管理"这一套从前到后看懂，其他都是套路。
5. **善用全局搜索**：在 IDE 里用 Ctrl+Shift+F 全局搜，找接口、找类、找配置都很快。

---

## 附录：常用命令速查

| 命令 | 作用 |
|---|---|
| `mvn clean install` | 编译打包所有模块 |
| `mvn spring-boot:run` | 启动后端（在 mars-starter 目录下） |
| `cd mars-ui && npm run dev` | 启动前端开发服务器 |
| `npm run build` | 前端打包（输出到后端 static 目录） |

---

> 这份文档会随着你的学习不断补充。看不懂的地方先记下来，跑一遍项目再回头看，很多疑惑会自然解开。加油！
